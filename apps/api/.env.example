# Copy to .env.local at the project root.
# The config package loads .env.local automatically from the workspace root.
#
# Required variables:

API_PORT=3001
DATABASE_URL=../../data/app.db
INTERNAL_API_KEY=
MINIAPP_CONTEXT_SECRET=
